# Memory

> Chronological action log. Hooks and AI append to this file automatically.
> Old sessions are consolidated by the daemon weekly.
| 00:08 | Codebase inventory for resume-builder | STATUS.md, cerebrum.md | Mapped stack, models, API routes, main UX flow | ~8k |
| 00:19 | Locked B2 storage decisions | cerebrum.md, STATUS.md | disk templates; PDF-only B2; presigned URLs; backup-only Resume rows | ~2k |
| 00:28 | generate-from-link ? B2 + JSON response | storage.ts, generate-from-link/route.ts, download route, env | company/jobTitle/resumeDownloadLink contract live | ~6k |
| 00:31 | Confirmed B2 creds via .env.local | .env.local | Names match storage.ts; file gitignored | ~1k |
| 00:39 | Diagnosed B2 403 InvalidAccessKeyId | .env.local, storage.ts | Wrong region us-west-004 vs us-east-005; upload OK after fix | ~4k |
| 00:42 | Renamed app metadata to Resume Builder | layout.tsx | title/description no longer Create Next App | ~0.5k |
| 08:08 | API key auth for generate-from-link | User.ts, apiKey.ts, routes, admin/dashboard UI | userId removed from body; X-API-Key header | ~8k |
| 11:38 | Add fileName to generate-from-link response | generate-from-link/route.ts | company, jobTitle, fileName, resumeDownloadLink | ~0.5k |
| 13:35 | Full PDF parity for generate-from-link | resumeHtml.ts, pdfFromHtml.ts, generate-from-link, pdf route | Shared templates + UI filename + fonts pipeline | ~12k |
| 22:48 | Phase 1 GitHub CI | ci.yml, package.json, eslint, User.ts | lint+typecheck+build workflow; local verify OK | ~6k |
| 00:19 | Atlas-only Docker compose + .env | docker-compose.yml, .env.example, mongodb.ts | Removed local mongo service; require MONGODB_URI | ~3k |
| 19:45 | Migrated sample codebase's new features into resume-builder | resumeHtml.ts, review/page.tsx, ProfileForm.tsx, Profile.ts, User.ts, BidDetail.ts, bid-details route, bids page, Nav.tsx, prompts.ts, profiles routes, admin users route+page, auth login/session | 10 PDF templates (was 3) gated to admin/premium via new `is_premium` flag; ported Bids tracking feature (BidDetail model, POST-on-download, /bids page); ported prompt verb-cap + buzzword rules. Left resume-generator's own advantages (B2 storage, API-key auth, Puppeteer concurrency guard, generate resilience) untouched — sample was behind on those. typecheck+lint+build all green | ~35k |
| 20:15 | Set `admin` user's `is_premium=true` in Atlas; dropped `jobDescription` from Bids | User doc (DB, one-off script), BidDetail.ts, api/bid-details/route.ts, bids/page.tsx, review/page.tsx, types/resume.ts, resume-generator/page.tsx | One-off mongoose script (created + deleted) flipped `is_premium` on the single `is_admin:true` user found. Removed `jobDescription` end-to-end from bid saving (model field, POST body, save call) and the now-pointless `ResumeReviewData.jobDescription` passthrough; Bids page lost its expand-row (nothing left to expand) | ~4k |
| 21:40 | Phase 2 CD implemented and pushed to `development` | .github/workflows/deploy.yml (new), README.md, VPS root authorized_keys, GitHub Actions secrets | Inspected live VPS first — found PM2+nginx, not Docker, running production. Bootstrapped ed25519 deploy key (VPS had password-auth only), pushed DEPLOY_SSH_KEY/DEPLOY_HOST/DEPLOY_USER as repo secrets via GitHub API, deleted local key copy. deploy.yml: workflow_run off green CI on master (+ workflow_dispatch w/ ref for manual redeploy/rollback) → SSH → git reset --hard → npm ci && npm run build → pm2 restart → health-check w/ auto-rollback on failure. Committed+pushed to development (e00724e); needs merge to master to activate — not merged, that's the user's call | ~18k |
